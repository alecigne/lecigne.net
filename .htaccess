RewriteEngine On

# Redirect plain HTTP requests to HTTPS.
RewriteCond %{SERVER_PORT} 80
RewriteRule (.*) https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]

# Keep old visual-feed URLs working after the rename.
RewriteRule ^blog(?:/(.*))?$ https://lecigne.net/feed/$1 [R=301,L,NE]
